import React from 'react';
import Card from '@material-ui/core/Card';
import CardContent from '@material-ui/core/CardContent';

class Home extends React.Component {
    render() {
        return (
            <Card>
                <CardContent>
                    WordCloud application based on React and Firebase
                </CardContent>
            </Card>
        );
    }
}

export default Home;